[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'uvicorn app.worker_server:app --host 0.0.0.0 --port $PORT & python -m arq app.workers.render_worker.WorkerSettings'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
